import FaceTimeType from './FaceTimeType';
import WFSerializationType from './WFSerializationType';

type WFFaceTimeType = {
  'Value': {
    'Type': FaceTimeType,
  },
  'WFSerializationType': WFSerializationType,
};

export default WFFaceTimeType;
